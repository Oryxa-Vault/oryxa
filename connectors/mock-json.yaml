# Works against `cargo run --bin mockagent`. No streaming, no open step —
# the minimum a connector can be.
name: mock-json
base: http://{{env.ORYXA_AGENT_HOST}}:9000

turn:
  method: POST
  path: /invoke
  body:
    prompt: "{{input}}"
  response:
    format: json
    text:
      - $.output
